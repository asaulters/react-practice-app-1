
import Form from "./Form";

const NewUser = (props) => {

    const userData = (enteredUserData) => {
        const newUserData = {
            ...enteredUserData,
            id: Math.random().toString()
        }
        
        
    }

}





export default NewUser;